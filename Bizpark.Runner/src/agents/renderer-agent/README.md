# Renderer Agent

Description: Renders website preview/build artifact from template + cmsData.

Expected Input:
- Template deployment config + cmsData

Expected Output:
- Rendered HTML/build output + preview URL

Status: TODO (placeholder only)
